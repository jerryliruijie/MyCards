# MyCards Web

## 常用命令

```bash
npm install
npm run dev
npm run lint
npm run test
```

当前线框阶段页面：
- `/` 仪表盘
- `/cards` 卡片列表
- `/cards/new` 新增卡片
- `/cards/[id]` 卡片详情
- `/cards/[id]/edit` 编辑卡片
- `/storage` 存储管理
